// src > scripts > utils > Formatter.util.js
/*

Aqui ficará uma classe responsável por fazer formatações de dados,
ou seja, terá qualquer formatador que seja útil em várias partes do código.

Um bom exemplo disso seria um formatador de número para preço em real:

Ex.:
    export default class Formatters {
        static numberToBRL(number) {
            return `R$${number.toFixed(2)}`;
        }
    }

*/
