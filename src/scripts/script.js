// src > scripts > script.js
/*

Aqui ficará as lógicas principais do seu projeto,
principalmente aquelas executadas ao carregar o seu website.

Além disso, será feita a importação de outros arquivos .js nesse arquivo.

Ex.:
    import { shoppingCart } from "../database/db.js";
    import HTMLController from "../scripts/controllers/HTML.controller.js";

    const cartHTML = HTMLController.generateShoppingCart(shoppingCart);
    document.body.appendChild(cartHTML);

*/
