// src > scripts > services > Api.service.js
/*

Aqui ficará uma classe responsável por fazer as resquisições de uma Api.

Detalhe, ela fará apenas as requisições,
quem manipulará o DOM será um controller ou o próprio script.js.

Ex.:

*/

export default class ApiService {
  static BASE_URL = "http://";

  static get() {
    // [...]
  }

  // [...]
}
